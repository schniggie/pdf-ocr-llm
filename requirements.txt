torch
torchvision
transformers
pillow
pdf2image
pyyaml
tqdm
accelerate
qwen-vl-utils
sentencepiece
protobuf
python-dotenv

# API and UI
fastapi
uvicorn
gradio
python-multipart

# Optional: Flash Attention 2 for faster inference (requires CUDA)
# pip install flash-attn

# System (install separately): poppler-utils (for pdf2image)
