torch>=2.0.0
torchvision>=0.15.0
transformers>=4.45.0
pillow>=10.0.0
pdf2image>=1.16.3
pyyaml>=6.0
tqdm>=4.65.0
accelerate>=0.27.0
qwen-vl-utils
sentencepiece>=0.1.99
protobuf>=3.20.0
python-dotenv>=1.0.0

# API and UI dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
gradio>=4.0.0
python-multipart>=0.0.6

# Optional: Flash Attention 2 for faster inference (requires CUDA)
# Uncomment and install separately if you have CUDA:
# flash-attn>=2.3.0

# System dependencies (install separately):
# - poppler-utils (for pdf2image)

